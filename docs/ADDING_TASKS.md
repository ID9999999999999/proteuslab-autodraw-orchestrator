# Adding Tasks

Tasks are in:

```text
src/proteus_lab_assistant/data/tasks.json
```

Use:

```bash
proteus-lab new-task --task my_task --title "My Task"
```

Task points are relative to the calibrated schematic origin.
